type UserSurvey {
  survey_id: Int
  user_id: String
  isComplete: Boolean
}

type Mutation {
  startSurvey(survey_id: Int!, user_id: Int!): UserSurvey
  completeSurvey(survey_id: Int!, user_id: Int!): UserSurvey
}
