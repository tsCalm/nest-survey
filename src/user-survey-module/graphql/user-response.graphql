type UserResponse {
  survey_id: Int
  question_id: Int
  user_id: Int
  user_answer: String
}

type Mutation {
  saveUserSelectOption(
    survey_id: Int!
    question_id: Int!
    user_id: Int!
    user_answer: String!
  ): UserResponse
}
